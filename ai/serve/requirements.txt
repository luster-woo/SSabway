# 추론 서버 전용. 학습에 필요한 것은 ../requirements.txt 에 있다.
#
# CPU 전용 서버(EC2)라면 torch 를 CPU 빌드로 먼저 설치한다.
# GPU 빌드는 CUDA 라이브러리까지 받아서 2GB 이상 커진다.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

torch>=2.11
torchvision>=0.26
ultralytics>=8.4
pillow>=11.0
numpy>=2.0

# 얼굴 검출(YuNet)에 cv2.FaceDetectorYN 이 필요하다 — 4.5.4 부터 들어왔다.
# ultralytics 가 이미 opencv 를 끌어오지만, 버전을 여기서 못 박아 둔다.
opencv-python>=4.10

# 실시간 번역 자막. 리눅스에서는 libasound2 와 libssl 이 있어야 import 된다
# (Dockerfile 의 apt 목록 참고). 마이크를 쓰지 않아도 SDK 가 링크한다.
azure-cognitiveservices-speech>=1.40

fastapi>=0.120
uvicorn[standard]>=0.38
python-multipart>=0.0.20
